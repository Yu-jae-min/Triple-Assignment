import colors from './constants/colors'
import flexbox from './constants/flexbox'
import animations from './constants/animations'

const theme = {
  colors,
  flexbox,
  animations,
}

export default theme
